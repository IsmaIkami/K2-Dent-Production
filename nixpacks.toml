# Nixpacks Configuration for Railway
# Note: Set Root Directory to "backend" in Railway Settings

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]

[start]
cmd = "node server.js"
