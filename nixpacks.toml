# Nixpacks Configuration for Railway
# Specifies that the Node.js app is in the backend directory

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm ci"]

[phases.build]

[start]
cmd = "cd backend && node server.js"
