# Nixpacks Configuration for Railway
# Build configuration for monorepo structure

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd backend && npm ci"]

[phases.build]

[start]
cmd = "node server.js"
